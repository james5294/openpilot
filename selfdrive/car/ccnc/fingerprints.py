from openpilot.selfdrive.car.ccnc.values import CAR

# Example fingerprint - this will need to be populated
FINGERPRINTS = {
  CAR.CCNC_MODEL_EXAMPLE: [{
    0: {1: 8, 2: 8, 3: 8}, # Example: Add actual message IDs and lengths
  }],
}

FW_VERSIONS = {
  CAR.CCNC_MODEL_EXAMPLE: {}, # To be filled
}
